:D
#find usr
whoami
#ipadress
ip a
#folder navi
cd
ls
pwd
../
#make folder
mkdir
#delete folder
rmdir
#delete target
rm -r

#check hw
lspci
lspci | grep -i <search term>
        #AMD, Intel, VGA, network, audio, usb, storage, sata
lspci -nn | grep -i <search term>
        #nn =  gives you device codes, useful for passthrough devices
		#grep = search, -i = ignore case sensitive, etc etc. 

#Permissions
ls -l
	# d - directory, r - read, w - write, x - execute
rwx rwx rwx # --- = off
--- --- ---
user group evryone

#compress file
tar -cvf <name.tar.gz> <file>
#decompress
tar -xvf <filename>
 
#tar -cvf helix.tar.gz helix.mp3 helix.mp4 

#grab file from target to host
scp <username@remote_host:/path/to/remote/file> </path/to/local/destination>
#send file from host to target
scp <file> <user>@<ip address>:/home/<user>
#ssh
ssh <usr>@<ipaddress>

#scp notes.bash cat77@10.0.0.240:/home/cat77/Projects
#scp cat.gif cat77@10.0.0.240:/home/cat77/Projects/bass
#scp cat.gif cat77@10.0.0.240:/home/cat77/Projects/tacotest
#scp helix.tar.gz cat77@10.0.0.240:/home/cat77/Projects

#Videos

#helix
https://www.youtube.com/watch?v=YkEeugn041M
#windows ssh 2025
https://www.youtube.com/watch?v=owebHznEGQ8

#windows commands
ipconfig
diskpart

#diskpart windows commands
set id=de94bba4-06d1-4d40-a16a-bfd50179d6ac gpt attributes=0x8000000000000001
	#sets recovery 
reagentc /enable
	#disable or info work as well
format quick fs=ntfs label="Recovery"
create partition primary size=1024
#  	# Manjaro commands #	#
sudo pacman -S openssh
#GPU switcher
sudo envycontrol -s hybrid
sudo envycontrol -s integrated 
#virt gpu passthrough ready
sudo virsh net-autostart default
sudo virsh net-list --all 
sudo virsh net-start default
sudo pacman -S virt-manager qemu libvirt edk2-ovmf dnsmasq bridge-utils openbsd-netcat iptables-nft ovmf
sudo systemctl status virtnetworkd.service
sudo systemctl status virtnetworkd.socket
sudo systemctl enable libvirtd.service
systemctl start virtqemud 
sudo systemctl enable virtqemud 
    #status or enable
echo drivetemp | sudo tee /etc/modules-load.d/drivetemp.conf


#.conf for gpuvirt passthrough 
sudo nano /etc/mkinitcpio.conf
vfio_pci vfio vfio_iommu_type1

sudo nano /etc/modprobe.d/vfio.conf
options vfio-pci ids=<YOUR:GPUID>,<ALSO:GPUAUDIO>

#grub info
sudo nano /etc/default/grub 
amdgpu.ppfeaturemask=0xffffffff amd_iommu=on iommu=pt kvm.ignore_msrs=1
sudo mkinitcpio -P
sudo update-grub
# 	# 

#git copy these notes. INTO CURRENT FOLDER. C:\ by default for windows. helps to copy the path from explorer. then for example use "cd  D:\Notes" or "cd C:\Users\mikufanfakeusernametestthanksforreadingthisfar\Downloads"
git clone https://github.com/mikufan2000/mikuLearn.git
